<div class="mb-4">
	<h4 class="devoured-burger-name">{{burger_name}}</h4>

	<!--Trash! burger button-->
	<button class="delete-burger btn btn-primary pt-1.5 pb-1.5" data-id="{{this.id}}"><i class="far fa-trash-alt mr-1"></i> Trash!</button>
</div>